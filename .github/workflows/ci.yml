on:
  workflow_dispatch:    # manual trigger only

jobs:
  noop:
    if: false           # always skip this job
    runs-on: ubuntu-latest
    steps:
      - name: CI disabled
        run: echo "This CI workflow is currently disabled."

# ——————————————————————————————————————————————————————————————————————
# Original CI config (commented out)
#
# name: CI
#
# on:
#   push:
#     branches: [main]
#   pull_request:
#     branches: [main]
#
# jobs:
#   build-and-test:
#     runs-on: ubuntu-latest
#     steps:
#       - name: Checkout repository
#         uses: actions/checkout@v3
#
#       - name: Set up Python
#         uses: actions/setup-python@v4
#         with:
#           python-version: '3.11'
#
#       - name: Install dependencies
#         run: |
#           pip install --upgrade pip
#           pip install -r mal/requirements.txt
#
#       - name: Run tests
#         working-directory: mal
#         run: pytest tests
# ——————————————————————————————————————————————————————————————————————
